# 範例 secrets 檔,複製為 .streamlit\secrets.toml 並填入實際值
# 注意:.streamlit\secrets.toml 必須加入 .gitignore,絕不推上 GitHub

GEMINI_API_KEY = "請填入 Gemini API Key"
PRIMARY_COACH_ID = "請填入 Users 工作表中 role=coach 的 user_id"

[gcp]
# Google Cloud Service Account JSON 欄位
# type = "service_account"
# project_id = "..."
# private_key = "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# client_email = "..."
# client_id = "..."
# auth_uri = "https://accounts.google.com/o/oauth2/auth"
# token_uri = "https://oauth2.googleapis.com/token"
# auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
# client_x509_cert_url = "..."

SPREADSHEET_ID = "請填入 Google Sheet 的 ID（在 URL /d/ 與 /edit 之間）"
